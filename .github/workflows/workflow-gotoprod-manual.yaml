on:
  workflow_call:
    inputs:
      webhookUri:
        required: false
        type: string
    secrets:
      KUBEWEBHOOK_TOKEN:
        required: true

jobs:
  production:
    name: Kontinuous Production Pipeline 🚀
    runs-on: ubuntu-latest
    environment: production
    steps:
      
      - name: Starting Deployment 🎬
        uses: socialgouv/kontinuous/.github/actions/deployment-starting@1.96.8
        id: deployment-starting
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Kontinuous Pipeline 🥷
        uses: socialgouv/kontinuous/.github/actions/deploy-via-webhook@1.96.8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          webhookToken: ${{ secrets.KUBEWEBHOOK_TOKEN }}
          webhookUri: ${{ inputs.webhookUri }}
          triggerWebhook: "true"
          environment: "prod"
      
      - name: Reporting 📜
        uses: socialgouv/kontinuous/.github/actions/deployment-reporting@1.96.8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          deployment_id: ${{ steps.deployment-starting.outputs.deployment_id }}