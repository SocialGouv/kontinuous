on:
  workflow_call:
    inputs:
      webhookUri:
        required: false
        type: string
    secrets:
      KUBEWEBHOOK_TOKEN:
        required: true

jobs:
  production:
    name: Kontinuous Production Pipeline 🚀
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: kontinuous pipeline
        uses: SocialGouv/kontinuous/gh-webhook-action@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          webhookToken: ${{ secrets.KUBEWEBHOOK_TOKEN }}
          webhookUri: ${{ inputs.webhookUri }}
          triggerWebhook: "true"
          environment: "prod"
      
      - name: ending
        uses: SocialGouv/kontinuous/.github/workflows/workflow-webhook-ending.yaml