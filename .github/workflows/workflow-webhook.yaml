on:
  workflow_call:
    inputs:
      webhookUri:
        required: false
        type: string
      triggerWebhook:
        required: false
        type: boolean
      environment:
        required: false
        type: string
    secrets:
      KUBEWEBHOOK_TOKEN:
        required: true

jobs:
  webhook:
    name: Kontinuous Pipeline (via webhook)
    runs-on: ubuntu-latest
    steps:

      - name: Starting Deployment 🎬
        uses: socialgouv/kontinuous/.github/actions/deployment-starting@v1.97.13
        id: deployment-starting
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Kontinuous Pipeline 🥷
        uses: socialgouv/kontinuous/.github/actions/deploy-via-webhook@v1.97.13
        continue-on-error: true
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          webhookToken: ${{ secrets.KUBEWEBHOOK_TOKEN }}
          webhookUri: ${{ inputs.webhookUri }}
          triggerWebhook: ${{ inputs.triggerWebhook }}
          environment: ${{ inputs.environment }}
      
      - name: Reporting 📜
        uses: socialgouv/kontinuous/.github/actions/deployment-reporting@v1.97.13
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          deployment_id: ${{ steps.deployment-starting.outputs.deployment_id }}