name: "Load config"
description: "Load remote kontinuous config from repo"
outputs:
  kontinuous_config:
    description: file path containing output of kontinuous remote config
  deployment_id:
    description: deployment id
runs:
  using: docker
  # image: Dockerfile
  image: docker://ghcr.io/socialgouv/kontinuous/gh-action-load-remote-config:v1.110.3