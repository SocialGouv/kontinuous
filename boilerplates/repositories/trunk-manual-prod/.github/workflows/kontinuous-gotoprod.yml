name: Production
on:
  push:
    tags:
      - v*

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}

jobs:
  deploy:
    uses: socialgouv/kontinuous/.github/workflows/workflow-gotoprod-manual.yaml@1.109.13-beta.1
    secrets: inherit
