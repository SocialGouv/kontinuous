{{- define "fabrique-helpers.ingress-secret" -}}
{{- if .Values.certSecretName }}
secretName: {{ or .Values.certSecretName (print (or .Values.component .Chart.Name) "-crt") }}
{{- else if .Values.global.isProd }}
secretName: {{ print (or .Values.component .Chart.Name) "-crt" }}
{{- else }}
secretName: "wildcard-crt"
{{- end }}
{{- end -}}