name: Production

on:
  push:
    tags:
      - v*

concurrency:
  group: production
  cancel-in-progress: true

jobs:
  follow-deployment:
    environment:
      name: production
      url: https://my-app.fabrique.social.gouv.fr
    uses: SocialGouv/kontinuous/.github/workflows/workflow-logs.yaml@master
    secrets: inherit
