name: Preproduction
on:
  push:
    branches:
      - master
      - main
    tags-ignore:
      - v*

concurrency:
  cancel-in-progress: true
  group: preproduction

jobs:
  follow-deployment:
    uses: SocialGouv/kontinuous/.github/workflows/workflow-logs.yaml@master
    secrets: inherit
