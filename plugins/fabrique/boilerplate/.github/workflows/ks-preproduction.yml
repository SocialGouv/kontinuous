name: Preproduction
on:
  push:
    branches:
      - master
      - main
    tags-ignore:
      - v*

concurrency:
  cancel-in-progress: true
  group: preproduction

jobs:
  follow-deployment:
    environment:
      name: preproduction
      url: https://my-app-preprod.dev.fabrique.social.gouv.fr
    uses: SocialGouv/kontinuous/.github/workflows/workflow-logs.yaml@v1
    secrets: inherit
