dependencies:
  fabrique:
    import: SocialGouv/kontinuous/plugins/fabrique

## enable this replacing {name-of-rancher-project-name} to use `npx kontinuous` cli from local environment
# webhook:
#   uri: https://webhook-{name-of-rancher-project-name}.fabrique.social.gouv.fr
